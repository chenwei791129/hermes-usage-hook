"""Provider implementations for usage footers.

Each module exposes a synchronous fetcher returning the shared normalized usage
structure consumed by ``usage.py``. Adding a provider is one new module here
plus one registry line in ``usage.py``.
"""
